---
name: "Lilith"
description: "Mentor and life coach."
dialog: "lilith"
model:
  graphics: "game/character/bunny/bunny"
  skin: "yellow"
  scale: 0.16
  blocking: false
  hitbox:
    type: rectangular
stats:
  level: 10
  health: 1000
  maxhealth: 1000
  movement: 210
spawn:
  jexl: "return true;"
death:
  jexl: "return true;"
tick:
  groovy: |
    for (creature : context.creatures().all()) {
      if (!creature.getName().equals('Lilith') && !creature.getName().equals('Gargazar')) {
        context.movement().follow(source, creature);
        break;
      }
    }