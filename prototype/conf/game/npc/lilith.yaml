---
name: "Illicit Lilith"
description: "Malevolent. Angry. Spirit."
dialog: "tutor"
model:
  graphics: "some sprite"
  scale: 1.0
  blocking: false
  hitbox:
    type: rectangular
stats:
  level: 10
  health: 1000.0
  maxhealth: 1000.0
  movement: 1.0
spawn:
  jexl: "return true;"
death:
  jexl: "return true;"
tick:
  groovy: |
    for (creature : context.creatures().all()) {
      if (!creature.getName().equals('Illicit Lilith')) {
        context.movement().follow(source, creature);
        break;
      }
    }