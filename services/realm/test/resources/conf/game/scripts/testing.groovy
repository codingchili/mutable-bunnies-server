import com.codingchili.instance.model.stats.Attribute

System.out.println("in potent_venom")

/*
creatures = context.creatures().radius(
        target.getVector().copy().setSize(256)
)

damage = damage + source.stats[Attribute.dexterity] / 2

creatures.each {
    console.log(it)

    amplified = target.stats[Attribute.maxhealth] * 0.02 + damage
    spells.damage(source, it, -amplified, ModifierType.poison)
}*/
